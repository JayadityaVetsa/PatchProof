# Scripts

This directory contains repository maintenance scripts. `check-docs.mjs` validates local Markdown links during CI.

Scripts must be reproducible, documented, and avoid destructive behavior by default.
